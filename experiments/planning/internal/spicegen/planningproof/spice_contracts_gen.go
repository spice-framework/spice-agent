//go:build !spice_generate

// Code generated by Spice. DO NOT EDIT.

package spicegen

import (
	fmt "fmt"
	strings "strings"
	time "time"

	agent "github.com/spice-framework/spice-agent/agent"
	planning "github.com/spice-framework/spice-agent/experiments/planning"
	composition "github.com/spice-framework/spice-agent/experiments/planning/internal/composition"
	spiceComposition "github.com/spice-framework/spice-agent/experiments/planning/internal/spicegen/planningproof/sources/internal_/composition"
	stage "github.com/spice-framework/spice-agent/stage"
	spicebean "github.com/spice-framework/spice/bean"
	spiceconfig "github.com/spice-framework/spice/config"
	spicelifecycle "github.com/spice-framework/spice/lifecycle"
)

const TargetID = spiceComposition.ApplicationTargetPlanningProof_9215e3bb

const (
	ExitSuccess = 0
	ExitFailure = 1
	ExitUsage   = 2
)

// Components is a typed snapshot of constructed singleton beans.
// It performs no reflection or string-based lookup.
type Components struct {
	// Provider0 is bean "planning.default".
	Provider0 planning.Planner
	// PlanningProofProvider is bean "planningProofProvider".
	PlanningProofProvider *composition.ProofProvider
	// PlanningProofDispatcher is bean "planningProofDispatcher".
	PlanningProofDispatcher *stage.Dispatcher
	// PlanningProofEngine is bean "planningProofEngine".
	PlanningProofEngine *agent.Engine
	// PlanningService is bean "planningService".
	PlanningService *planning.Service
	// Proof is bean "proof".
	Proof *composition.Proof
}

// BeanOverrides provides compile-time-typed singleton replacements.
// Replacements use the normal generated cleanup and rollback path.
type BeanOverrides struct {
	// Provider0 replaces bean "planning.default".
	Provider0 spicebean.Override[planning.Planner]
	// PlanningProofProvider replaces bean "planningProofProvider".
	PlanningProofProvider spicebean.Override[*composition.ProofProvider]
	// PlanningProofDispatcher replaces bean "planningProofDispatcher".
	PlanningProofDispatcher spicebean.Override[*stage.Dispatcher]
	// PlanningProofEngine replaces bean "planningProofEngine".
	PlanningProofEngine spicebean.Override[*agent.Engine]
	// PlanningService replaces bean "planningService".
	PlanningService spicebean.Override[*planning.Service]
	// Proof replaces bean "proof".
	Proof spicebean.Override[*composition.Proof]
}

// BeanOverrideLayer is one named immutable override composition layer.
// Layers are applied in order; a later layer deliberately replaces an earlier value.
type BeanOverrideLayer struct {
	Name      string
	Overrides BeanOverrides
}

// ComposeBeanOverrides validates and deterministically composes named layers.
// It never mutates a running application or performs runtime bean lookup.
func ComposeBeanOverrides(layers ...BeanOverrideLayer) (BeanOverrides, error) {
	result := BeanOverrides{}
	seen := make(map[string]int, len(layers))
	for index, layer := range layers {
		if layer.Name == "" || strings.TrimSpace(layer.Name) != layer.Name {
			return BeanOverrides{}, fmt.Errorf("compose bean overrides: layer %d requires a non-empty name without surrounding whitespace", index)
		}
		if previous, duplicate := seen[layer.Name]; duplicate {
			return BeanOverrides{}, fmt.Errorf("compose bean overrides: layer %q repeats layer %d", layer.Name, previous)
		}
		seen[layer.Name] = index
		if layer.Overrides.Provider0.Enabled() {
			result.Provider0 = layer.Overrides.Provider0
		}
		if layer.Overrides.PlanningProofProvider.Enabled() {
			result.PlanningProofProvider = layer.Overrides.PlanningProofProvider
		}
		if layer.Overrides.PlanningProofDispatcher.Enabled() {
			result.PlanningProofDispatcher = layer.Overrides.PlanningProofDispatcher
		}
		if layer.Overrides.PlanningProofEngine.Enabled() {
			result.PlanningProofEngine = layer.Overrides.PlanningProofEngine
		}
		if layer.Overrides.PlanningService.Enabled() {
			result.PlanningService = layer.Overrides.PlanningService
		}
		if layer.Overrides.Proof.Enabled() {
			result.Proof = layer.Overrides.Proof
		}
	}
	return result, nil
}

type Application struct {
	coordinator     *spicelifecycle.Coordinator
	hooks           []spicelifecycle.Hook
	shutdownTimeout time.Duration
	components      Components
}

type ApplicationOptions struct {
	Overrides                 BeanOverrides
	Profiles                  []string
	Sources                   []spiceconfig.Source
	AllowUnknownConfiguration bool
	Observers                 []spicelifecycle.Observer
}
