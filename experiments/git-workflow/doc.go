// Package gitworkflow is an experimental, application-owned Git tool set.
// It deliberately exposes only inspection and an explicitly approved commit
// of the already-staged index.
package gitworkflow

// ContractVersion identifies the removable experiment's semantic boundary.
const ContractVersion = "spice.agent.git-workflow/v1alpha1"
