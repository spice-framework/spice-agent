//go:build !spice_generate

// Code generated by Spice. DO NOT EDIT.

package spicegen

import (
	fmt "fmt"
	strings "strings"
	time "time"

	gitworkflow "github.com/spice-framework/spice-agent/experiments/git-workflow"
	composition "github.com/spice-framework/spice-agent/experiments/git-workflow/internal/composition"
	spiceComposition "github.com/spice-framework/spice-agent/experiments/git-workflow/internal/spicegen/gitworkflowproof/sources/internal_/composition"
	stage "github.com/spice-framework/spice-agent/stage"
	tool "github.com/spice-framework/spice-agent/tool"
	spicebean "github.com/spice-framework/spice/bean"
	spiceconfig "github.com/spice-framework/spice/config"
	spicelifecycle "github.com/spice-framework/spice/lifecycle"
)

const TargetID = spiceComposition.ApplicationTargetGitWorkflowProof_a4161688

const (
	ExitSuccess = 0
	ExitFailure = 1
	ExitUsage   = 2
)

// Components is a typed snapshot of constructed singleton beans.
// It performs no reflection or string-based lookup.
type Components struct {
	// GitBackend is bean "gitBackend".
	GitBackend gitworkflow.Backend
	// Provider1 is bean "git.inspect".
	Provider1 tool.Tool
	// GitAuthorityStore is bean "gitAuthorityStore".
	GitAuthorityStore *gitworkflow.AuthorityStore
	// Provider3 is bean "git.commit_staged".
	Provider3 tool.Tool
	// GitCommitGuard is bean "gitCommitGuard".
	GitCommitGuard stage.ToolDispatchGuard
	// Proof is bean "proof".
	Proof *composition.Proof
}

// BeanOverrides provides compile-time-typed singleton replacements.
// Replacements use the normal generated cleanup and rollback path.
type BeanOverrides struct {
	// GitBackend replaces bean "gitBackend".
	GitBackend spicebean.Override[gitworkflow.Backend]
	// Provider1 replaces bean "git.inspect".
	Provider1 spicebean.Override[tool.Tool]
	// GitAuthorityStore replaces bean "gitAuthorityStore".
	GitAuthorityStore spicebean.Override[*gitworkflow.AuthorityStore]
	// Provider3 replaces bean "git.commit_staged".
	Provider3 spicebean.Override[tool.Tool]
	// GitCommitGuard replaces bean "gitCommitGuard".
	GitCommitGuard spicebean.Override[stage.ToolDispatchGuard]
	// Proof replaces bean "proof".
	Proof spicebean.Override[*composition.Proof]
}

// BeanOverrideLayer is one named immutable override composition layer.
// Layers are applied in order; a later layer deliberately replaces an earlier value.
type BeanOverrideLayer struct {
	Name      string
	Overrides BeanOverrides
}

// ComposeBeanOverrides validates and deterministically composes named layers.
// It never mutates a running application or performs runtime bean lookup.
func ComposeBeanOverrides(layers ...BeanOverrideLayer) (BeanOverrides, error) {
	result := BeanOverrides{}
	seen := make(map[string]int, len(layers))
	for index, layer := range layers {
		if layer.Name == "" || strings.TrimSpace(layer.Name) != layer.Name {
			return BeanOverrides{}, fmt.Errorf("compose bean overrides: layer %d requires a non-empty name without surrounding whitespace", index)
		}
		if previous, duplicate := seen[layer.Name]; duplicate {
			return BeanOverrides{}, fmt.Errorf("compose bean overrides: layer %q repeats layer %d", layer.Name, previous)
		}
		seen[layer.Name] = index
		if layer.Overrides.GitBackend.Enabled() {
			result.GitBackend = layer.Overrides.GitBackend
		}
		if layer.Overrides.Provider1.Enabled() {
			result.Provider1 = layer.Overrides.Provider1
		}
		if layer.Overrides.GitAuthorityStore.Enabled() {
			result.GitAuthorityStore = layer.Overrides.GitAuthorityStore
		}
		if layer.Overrides.Provider3.Enabled() {
			result.Provider3 = layer.Overrides.Provider3
		}
		if layer.Overrides.GitCommitGuard.Enabled() {
			result.GitCommitGuard = layer.Overrides.GitCommitGuard
		}
		if layer.Overrides.Proof.Enabled() {
			result.Proof = layer.Overrides.Proof
		}
	}
	return result, nil
}

type Application struct {
	coordinator     *spicelifecycle.Coordinator
	hooks           []spicelifecycle.Hook
	shutdownTimeout time.Duration
	components      Components
}

type ApplicationOptions struct {
	Overrides                 BeanOverrides
	Profiles                  []string
	Sources                   []spiceconfig.Source
	AllowUnknownConfiguration bool
	Observers                 []spicelifecycle.Observer
}
