//go:build !spice_generate

// Code generated by Spice. DO NOT EDIT.

package spicegen

import (
	fmt "fmt"
	strings "strings"
	time "time"

	agent "github.com/spice-framework/spice-agent/agent"
	daemon "github.com/spice-framework/spice-agent/daemon"
	event "github.com/spice-framework/spice-agent/event"
	telemetry "github.com/spice-framework/spice-agent/experiments/telemetry"
	composition "github.com/spice-framework/spice-agent/experiments/telemetry/internal/composition"
	spiceComposition "github.com/spice-framework/spice-agent/experiments/telemetry/internal/spicegen/telemetryproof/sources/internal_/composition"
	stage "github.com/spice-framework/spice-agent/stage"
	spicebean "github.com/spice-framework/spice/bean"
	spiceconfig "github.com/spice-framework/spice/config"
	spicelifecycle "github.com/spice-framework/spice/lifecycle"
)

const TargetID = spiceComposition.ApplicationTargetTelemetryProof_2350e436

const (
	ExitSuccess = 0
	ExitFailure = 1
	ExitUsage   = 2
)

// Components is a typed snapshot of constructed singleton beans.
// It performs no reflection or string-based lookup.
type Components struct {
	// TelemetryConfig is bean "telemetryConfig".
	TelemetryConfig telemetry.Config
	// TelemetryMailbox is bean "telemetryMailbox".
	TelemetryMailbox *event.BestEffortObserver
	// TelemetryProofProvider is bean "telemetryProofProvider".
	TelemetryProofProvider *composition.ProofProvider
	// TelemetryProofLifecycle is bean "telemetryProofLifecycle".
	TelemetryProofLifecycle *composition.ProofLifecycle
	// TelemetryExporter is bean "telemetryExporter".
	TelemetryExporter telemetry.Exporter
	// TelemetryProcessor is bean "telemetryProcessor".
	TelemetryProcessor *telemetry.Processor
	// TelemetryHealth is bean "telemetryHealth".
	TelemetryHealth daemon.HealthSource
	// TelemetryProofDispatcher is bean "telemetryProofDispatcher".
	TelemetryProofDispatcher *stage.Dispatcher
	// TelemetryProofEngine is bean "telemetryProofEngine".
	TelemetryProofEngine *agent.Engine
	// Proof is bean "proof".
	Proof *composition.Proof
}

// BeanOverrides provides compile-time-typed singleton replacements.
// Replacements use the normal generated cleanup and rollback path.
type BeanOverrides struct {
	// TelemetryConfig replaces bean "telemetryConfig".
	TelemetryConfig spicebean.Override[telemetry.Config]
	// TelemetryMailbox replaces bean "telemetryMailbox".
	TelemetryMailbox spicebean.Override[*event.BestEffortObserver]
	// TelemetryProofProvider replaces bean "telemetryProofProvider".
	TelemetryProofProvider spicebean.Override[*composition.ProofProvider]
	// TelemetryProofLifecycle replaces bean "telemetryProofLifecycle".
	TelemetryProofLifecycle spicebean.Override[*composition.ProofLifecycle]
	// TelemetryExporter replaces bean "telemetryExporter".
	TelemetryExporter spicebean.Override[telemetry.Exporter]
	// TelemetryProcessor replaces bean "telemetryProcessor".
	TelemetryProcessor spicebean.Override[*telemetry.Processor]
	// TelemetryHealth replaces bean "telemetryHealth".
	TelemetryHealth spicebean.Override[daemon.HealthSource]
	// TelemetryProofDispatcher replaces bean "telemetryProofDispatcher".
	TelemetryProofDispatcher spicebean.Override[*stage.Dispatcher]
	// TelemetryProofEngine replaces bean "telemetryProofEngine".
	TelemetryProofEngine spicebean.Override[*agent.Engine]
	// Proof replaces bean "proof".
	Proof spicebean.Override[*composition.Proof]
}

// BeanOverrideLayer is one named immutable override composition layer.
// Layers are applied in order; a later layer deliberately replaces an earlier value.
type BeanOverrideLayer struct {
	Name      string
	Overrides BeanOverrides
}

// ComposeBeanOverrides validates and deterministically composes named layers.
// It never mutates a running application or performs runtime bean lookup.
func ComposeBeanOverrides(layers ...BeanOverrideLayer) (BeanOverrides, error) {
	result := BeanOverrides{}
	seen := make(map[string]int, len(layers))
	for index, layer := range layers {
		if layer.Name == "" || strings.TrimSpace(layer.Name) != layer.Name {
			return BeanOverrides{}, fmt.Errorf("compose bean overrides: layer %d requires a non-empty name without surrounding whitespace", index)
		}
		if previous, duplicate := seen[layer.Name]; duplicate {
			return BeanOverrides{}, fmt.Errorf("compose bean overrides: layer %q repeats layer %d", layer.Name, previous)
		}
		seen[layer.Name] = index
		if layer.Overrides.TelemetryConfig.Enabled() {
			result.TelemetryConfig = layer.Overrides.TelemetryConfig
		}
		if layer.Overrides.TelemetryMailbox.Enabled() {
			result.TelemetryMailbox = layer.Overrides.TelemetryMailbox
		}
		if layer.Overrides.TelemetryProofProvider.Enabled() {
			result.TelemetryProofProvider = layer.Overrides.TelemetryProofProvider
		}
		if layer.Overrides.TelemetryProofLifecycle.Enabled() {
			result.TelemetryProofLifecycle = layer.Overrides.TelemetryProofLifecycle
		}
		if layer.Overrides.TelemetryExporter.Enabled() {
			result.TelemetryExporter = layer.Overrides.TelemetryExporter
		}
		if layer.Overrides.TelemetryProcessor.Enabled() {
			result.TelemetryProcessor = layer.Overrides.TelemetryProcessor
		}
		if layer.Overrides.TelemetryHealth.Enabled() {
			result.TelemetryHealth = layer.Overrides.TelemetryHealth
		}
		if layer.Overrides.TelemetryProofDispatcher.Enabled() {
			result.TelemetryProofDispatcher = layer.Overrides.TelemetryProofDispatcher
		}
		if layer.Overrides.TelemetryProofEngine.Enabled() {
			result.TelemetryProofEngine = layer.Overrides.TelemetryProofEngine
		}
		if layer.Overrides.Proof.Enabled() {
			result.Proof = layer.Overrides.Proof
		}
	}
	return result, nil
}

type Application struct {
	coordinator     *spicelifecycle.Coordinator
	hooks           []spicelifecycle.Hook
	shutdownTimeout time.Duration
	components      Components
}

type ApplicationOptions struct {
	Overrides                 BeanOverrides
	Profiles                  []string
	Sources                   []spiceconfig.Source
	AllowUnknownConfiguration bool
	Observers                 []spicelifecycle.Observer
}
